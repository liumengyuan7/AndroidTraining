<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.community.dao.CommentMapper">
    <resultMap id="dynamicMap" type="com.entity.Comment">
        <id property="id" column="id" />
        <result property="userId" column="user_id" />
        <result property="content" column="comment_content" />
    </resultMap>
    <insert id="insertComment">
        insert into comment(dynamics_id, user_id, comment_content)
        values(#{dynamicId},#{userId},#{content})
    </insert>
  <!--  <select id="selectCommnetByDynamicId" resultType="com.entity.Comment">
        select  c.user_id,c.comment_content
        from comment c
        where c.dynamics_id = #{dynamicId}
    </select>-->
</mapper>