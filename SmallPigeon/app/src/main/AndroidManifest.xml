<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.smallpigeon">
    <!-- 用于访问wifi网络信息，wifi信息会用于进行网络定位 -->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" /> <!-- 获取运营商信息，用于支持提供运营商信息相关的接口 -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" /> <!-- 这个权限用于获取wifi的获取权限，wifi信息会用来进行网络定位 -->
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" /> <!-- 写入扩展存储，向扩展卡写入数据，用于写入对象存储BOS数据 -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" /> <!-- 访问网络，网络定位需要上网 -->
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE" /> <!-- Android O之后开启前台服务需要申请该权限 -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" /> <!-- Android Q之后，后台定位需要申请该权限 -->
    <uses-permission android:name="android.permission.INTERNET" /> <!-- 以下不是鹰眼SDK需要的基础权限，可选 -->
    <!-- 用于加快GPS首次定位，可选权限，非必须 -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" /> <!-- 用于Android M及以上系统，申请加入忽略电池优化白名单，可选权限，非必须 -->
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_LOCATION_EXTRA_COMMANDS" />
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.MOUNT_UNMOUNT_FILESYSTEMS"
        tools:ignore="ProtectedPermissions" />
    <uses-permission android:name="android.permission.WRITE_MEDIA_STORAGE"
        tools:ignore="ProtectedPermissions" />
    <application
        android:name=".TrackApplication"
        android:allowBackup="true"
        android:icon="@mipmap/biao"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/NoTitleTheme"
        android:usesCleartextTraffic="true">
        <activity android:name=".Run.FinishRunActivity"></activity>
        <activity android:name=".My.Paihang" />
        <activity android:name=".My.update_email" />
        <activity android:name=".My.update_pwd_final" />
        <activity android:name=".My.update_pwd" />
        <activity android:name=".My.anquan" />
        <activity android:name=".LoginOrRegister.ResetPwd" />
        <activity android:name=".LoginOrRegister.ForgetPassword" />
        <activity android:name=".My.Personal_center_More" />
        <activity android:name=".My.Personal_center_updateUserNickname" />
        <activity android:name=".My.Personal_centet_updateUserImg" />
        <activity android:name=".LoginOrRegister.RegisterActivity" />
        <activity android:name=".LoginOrRegister.LoginActivity" />
        <activity android:name=".My.PersonalCenter" />
        <activity android:name=".Run.RemachingActivity" />
        <activity android:name=".Run.MachingActivity" />
        <activity android:name=".BaiduMap.activity.TracingActivity"
            android:screenOrientation="portrait"/><!--只允许竖屏显示-->
        <activity android:name=".BaiduMap.activity.TracingOptionsActivity" />
        <activity android:name=".BaiduMap.activity.BaseActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="com.baidu.lbsapi.API_KEY"
            android:value="tGSM2HnpdI65l0aTDEaUpn81WzDUtvOo" />

        <service
            android:name="com.baidu.trace.LBSTraceService"
            android:enabled="true"
            android:exported="true"
            android:process=":remote" />
    </application>
</manifest>